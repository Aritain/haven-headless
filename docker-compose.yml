services:
  haven-headless:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${HOST_PORT:-5001}:5000"
    environment:
      APP_DATA_DIR: /data
    volumes:
      - haven_headless_data:/data
    restart: unless-stopped

volumes:
  haven_headless_data:
